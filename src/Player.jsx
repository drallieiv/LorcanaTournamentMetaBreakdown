class Player {
  constructor(username, firstName, lastName, playerName, colors) {
    this.Username = username;
    this.FirstName = firstName;
    this.LastName = lastName;
    this.PlayerName = playerName;
    this.Colors = colors;
  }
}

export default Player;
